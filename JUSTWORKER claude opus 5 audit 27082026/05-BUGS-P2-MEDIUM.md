# 05 · Bugs — P2 Medium

[← Index](./00-README.md) · Prev: [04 P1 High](./04-BUGS-P1-HIGH.md) · Next: [06 Missing Work](./06-MISSING-WORK.md)

**Definition of P2:** Real defects with a known workaround, latent risk, performance inefficiencies, accessibility flaws, or code hygiene issues. 59 findings (B-34 through B-92).

### Tier 2 — medium

| # | Finding | Where | Fix |
| --- | --- | --- | --- |
| B-34 | Checkout idempotency key never rotates across retries; payment-init key derives from it. After a failed `payments/init` the user can retry but replays the same key, so the retry can never succeed. | `hub/checkout/page.tsx:50, 231, 263` | Mint a fresh key per `handleCheckout` invocation; separate key for payment init; keep it stable only while retrying an identical payload. |
| B-35 | 7 foreign keys still unindexed (prior audit claimed 203/203). Verified via corrected `pg_constraint`/`pg_index` predicate. | `pd_ads_campaign_placement.placement_id`, `pd_ads_coupon_redemption.store_id`, `pd_digital_download.product_id`, `pd_email_template_style.store_id`, `pd_store_delivery_proof.store_id`, `pd_storefront_category.parent_id`, `pd_storefront_customer_session.store_id` | `CREATE INDEX CONCURRENTLY` + mirror migration. |
| B-36 | RLS enabled on all 121 `pd_*` tables with **0 policies** (deny-by-default), but the 5 `admin_note*` tables have RLS **off** — they don't match the `pd_%` pattern the migration used. | `migrations/sql/086_enable_rls_pd_tables.sql` | Enable RLS on `admin_note*` too. |
| B-37 | 11 AI jobs stuck: 10 `queued` since May 2026, 1 `processing` since 2026-08-18. No stuck-job reaper. `ai.worker.ts:244` consumes credits **after** the work, so a crash between gives away paid output free. `ai-queue.ts` sets no `attempts`. | `ai.worker.ts`, `ai-queue.ts` | Reserve credits before the call, settle/refund after, keyed by `job_id`; add a reaper for `processing` older than N minutes; requeue or fail the 10 orphans. |
| B-38 | `ai.worker.ts` and `ai-tagger.worker.ts` both consume `pd_ai_queue`; whichever wins decides behaviour. `ai.worker.ts:216` calls `markProcessing(job_id)` for tagging jobs that may have no job row. | both workers | One consumer per queue, or separate queues. |
| B-39 | Webhook delivery: one failing subscription causes re-delivery to all the others on retry (`webhook.worker.ts:50-71`); unbounded `response.text()` (`:116`); stale `consecutive_failures` counter (`:161`); signature has no timestamp so deliveries are replayable forever (`:87`); `pd_webhook_subscription.secret` stored plaintext. | `workers/webhook.worker.ts`, `api/vendor.route.ts:245` | One job per subscription with `jobId: ${key}:${subId}`; read ≤8 KB; use `UPDATE … RETURNING`; sign `timestamp.body` + send `X-PD-Timestamp`; encrypt the secret. |
| B-40 | `payout.worker.ts:55-72` — after `releaseDueFunds()` returns > 0, it selects **every** wallet with no `WHERE` and emits `WALLET_FUNDS_AVAILABLE` for all of them → notification + email to vendors whose balance didn't change, every 15 minutes. Auto-payout (`:74-113`) reads the amount outside the transaction and has no idempotency key. | `workers/payout.worker.ts` | Return affected `store_id`s from `releaseDueFunds`; compute the amount inside the withdrawing transaction; add `autopayout:${store}:${date}` key. |
| B-41 | Daily digest is an unbounded per-buyer N+1 in one job with no `jobId` on the email enqueue → a retry re-emails everyone. `notification-batch.service.ts` static Maps have no size cap and fall back to per-process `setTimeout`, so a Redis outage dispatches each batch N times. | `notification-batch.service.ts:36-37, 113-129, 321-373` | Paginate + fan out child jobs; `jobId: digest:${buyer}:${date}`; Lua script for the read-delete-rewrite; cap the maps. |
| B-42 | Subscription expiry warnings re-sent on retry (no dedupe); `subscriptionService` caches plan limits in a process-local `Map` cleared only by three admin routes — stale for up to a process lifetime on other instances. | `workers/subscription.worker.ts:107-155`, `services/subscription.service.ts:39-59` | `alert_key` uniqueness (the pattern `ads.service.ts:829` uses); move the cache to Redis with a short TTL or a pub/sub invalidation. |
| B-43 | One shared ioredis connection for 10 BullMQ Workers, all Queues, **and** application commands (sessions, 2FA, reset tokens, rate limiting, maintenance config). Blocking worker commands queue ahead of app commands — which is why `withRedisTimeout` and `withLoginRedisTimeout` exist. `maxRetriesPerRequest: null` (BullMQ requirement) means app commands never give up. | `db/redis.ts:11-21`, `main.ts:542-553` | Dedicated connection per Worker, one for Queues, one app client with `maxRetriesPerRequest: 2` + `commandTimeout`. |
| B-44 | Rate-limit and maintenance-allowlist keys derive from client-settable headers (`cf-connecting-ip`, `x-real-ip`, first XFF element). Rotating a forged header defeats `authRateLimit`, `adsEventRateLimit`, `apiRateLimit` and the maintenance bypass. Redis outage → limiter fails open **and** login lockout is skipped entirely. | `middlewares/index.ts:314-326`, `maintenance.middleware.ts:102-113`, `rate-limit-store.ts:49-76`, `auth.service.ts:102-109` | Set `trust proxy` to the real hop count and use `req.ip`; if an edge header is required, allowlist the connecting socket. Fall back to a bounded `MemoryStore` rather than unlimited; fail **closed** for `/auth/login` and `/2fa/verify`; emit a metric when the fallback engages. |
| B-45 | 2FA challenge has no attempt limit and survives failures (`redis.del` only on success). 6 digits, `window=1`, 5-minute TTL, unlimited attempts. Recovery codes are 40-bit (`randomHex(5)`) behind unsalted SHA-256 and are a full 2FA bypass **and** a 2FA-disable credential. TOTP codes are replayable within their window. 2FA enrol/disable don't require the password. | `auth.service.ts:78-83, 458-525, 702-714`, `utils/totp.ts:67-76` | `INCR` attempts on the challenge key, delete after 5; `randomHex(16)` + bcrypt/argon2 or keyed HMAC; persist the accepted TOTP counter; require the current password for enrol/disable. |
| B-46 | No revocation check on access tokens. `revokeSession` and `logout` revoke refresh tokens; the 15-minute access token keeps working, and `pd_user.is_active=false` is only consulted on refresh. `/socket-token` (`auth.route.ts:468-482`) re-mints a fresh 15-minute token from the presented one **without any session check**, so revocation can be extended indefinitely. 23 live non-revoked sessions. | `middlewares/index.ts:83-104`, `auth.route.ts:468` | Check `pd_user_session.revoked_at IS NULL AND expires_at > NOW()` (Redis-cached) in `requireAuth`, or a revoked-`session_id` denylist with a 15-min TTL. Same check on `/socket-token`. |
| B-47 | Refresh rotation has no reuse detection — a revoked token throws and nothing else happens, so token theft is undetectable. Storefront rotation is in-place on the same row. `changePassword` (storefront) doesn't revoke sessions. | `auth.service.ts:328-397`, `storefront-auth.service.ts:283-312, 550-571` | On a revoked-token presentation, revoke the whole session family, record a `refresh_reuse_detected` event, alert. |
| B-48 | Verification token appears in `req.originalUrl` → written by `accessLog` at info **and** persisted to `pd_audit_log.action` + `metadata.path`. `redactBody` only inspects body keys. | `auth.route.ts:633-640`, `middlewares/index.ts:40-57`, `audit-log.middleware.ts:107-137` | Accept the token in a POST body; strip query strings from logged paths. |
| B-49 | Email templates interpolate variables into HTML **unescaped** — `product_name`, `reason`, `store_name`, `tracking_number`, `message`, `device_label`, `ip`. `escapeHtml` exists at `:40` and is applied only to branding/CTA text. `email-template.service.ts:107-109` escapes correctly, so the built-in path is the outlier. Recipient addresses + 200-char body previews logged at info. | `workers/email.worker.ts:128-132, 179-330, 363-377` | Route every `v()` through `escapeHtml`; mask logged recipients. |
| B-50 | CSRF exemptions are **substring** matches on the path (`req.path.includes('/callback')`, `/webhook/`, `/cart/sync`, `/shipping/rates`). Any future route containing those substrings silently loses protection. Also: `POST /shipping/smart-quotes` and `/rates` are CSRF-exempt *and* unauthenticated. | `middlewares/csrf.middleware.ts:61-71`, `api/shipping.route.ts:154-173` | Exact prefix matching, or mark exempt routes explicitly at mount time. Add rate limiting to the public shipping quote endpoints. |
| B-51 | AES-256-GCM has no key version, key id, or AAD, so `PD_ENCRYPTION_KEY` cannot be rotated without a bulk re-encrypt, and a `pd_store.payment_config` blob copied to another store row decrypts fine. `getKey` accepts any 64-char string (non-hex silently yields a short buffer). `ai-config.service.ts:71-88` `safeDecrypt` **returns the raw ciphertext as a plaintext API key** when decryption fails and the string is ≥ 8 chars — 8 live AI jobs failed with `Unsupported state or unable to authenticate data`. | `utils/crypto.ts:20-50`, `ai-config.service.ts:71-88` | Version+key-id header, AAD bound to the owning row id, re-encrypt migration path; validate `/^[0-9a-f]{64}$/i` at boot; remove the raw-passthrough fallback and surface a "re-enter your API key" error instead. |
| B-52 | Presigned S3 uploads carry only `ContentType` — the URL accepts an object of **any size**. The 110 MB cap applies only to the local mock path. Download presign expiry can exceed the SigV4 7-day maximum (`download_expires_hours` up to 8760 × 3600). | `utils/s3.ts:35-54`, `storefront-account.route.ts:272-276` | Presigned POST policy with `content-length-range`; clamp expiry to `min(hours*3600, 604800)` and cap the product field at 168 h. |
| B-53 | Admin-internal support-ticket **attachments** leak to the seller: `getSellerTicket` filters messages by `is_internal=false` but the attachment query has no filter, and the table has no visibility column. `file_url` is stored unvalidated (`z.string().max(2000)` accepts `javascript:`). Notifications fired inside transactions on the pool (survive rollback, unhandled rejections). | `support-ticket.service.ts:159-170, 197-205, 381-386`, `api/support.route.ts:47-53` | Add `visibility` to the attachment table (mirror `pd_report_attachments`), or join to the parent message; use `urlOrPathSchema`; dispatch notifications after commit. |
| B-54 | Chat: 2 extra DB round-trips per message just for limits (`getChatLimits` query + `COUNT(*)` rate limit); attachment ACL uses `m.attachments @> $1::jsonb` with no GIN index (sequential scan per file access); the unread-count subquery is injected via `.replace('SELECT\n    c.*,', …)` and silently no-ops if the template literal is reformatted. | `chat.service.ts:206-237, 776-786, 988-1007` | Cache limits; move the counter to Redis; add `USING gin (attachments jsonb_path_ops)`; build the query explicitly. |
| B-55 | Any seller can bust **any** store's ISR cache — `/api/storefront/revalidate` authenticates the caller as seller/admin then revalidates whatever `hostnames[]` it was given, with no ownership check. `page-builder/revalidate/route.ts:36-38` does check ownership; the asymmetry looks unintentional. | `frontend/src/app/api/storefront/revalidate/route.ts:23-72` | Resolve each hostname to a store and require `store_id` match (or admin). Also the known-open backlog item "scope seller-supplied hostnames". |
| B-56 | Theme customization is stored as `z.record(z.unknown())` and flows unchecked into `--tc-*` custom properties and string-concatenated colours (`` `${primaryColor}20` ``). No HTML injection (React uses CSSOM) but a non-hex value silently breaks every derived colour; unknown `heroStyle`/`layoutVariation`/`gridDensity` makes sections vanish. `getThemePreviewData` spreads `...store.settings` — leaking `seller_type_change_request`, `draft_*`, maintenance text — bypassing the `publicStorefrontSettings` projection every other public path uses. Seller `map_embed_url` becomes an unvalidated `<iframe src>` (`safeGoogleMapEmbedUrl` exists and is used elsewhere). | `store.route.ts:63-88, 1155-1166`, `store.service.ts:807-878`, `lib/themes.ts:149-158`, `StorefrontFooter.tsx:183-195` | Validate colours against `/^#[0-9a-fA-F]{3,8}$/` and layout values against `z.enum` on write; project preview data through `publicStorefrontSettings`; run both footer iframes through `safeGoogleMapEmbedUrl`. |
| B-57 | Page-builder draft/publish leaks `title` and `slug` — both are written to the **live** columns immediately while every other field goes to `draft_*` and is promoted only on publish. Renaming a page in the editor changes the live title, nav label and URL. Public read endpoints (`store.route.ts:1307-1379`) check only the platform-wide flag — no `has_page_builder` plan check and no store `status` check, so a downgraded or suspended store keeps serving custom pages. | `page-builder.service.ts:835-861`, `store.route.ts:1307-1379` | Add `draft_title`/`draft_slug` and promote on publish; join `pd_store` and check `status='verified' AND is_verified` plus the plan flag in the three read paths. |
| B-58 | Page-builder analytics ingest is unauthenticated and unthrottled — anyone can inflate the `views_30d`/`cta_clicks_30d` the seller dashboard reports. | `api/analytics.route.ts:74-109` | Dedicated limiter keyed on `clientBucketKey` + dedupe on `visitor_hash` + page per window. |
| B-59 | Store suspension reason is collected in the UI, sent, read as `req.body.reason`, passed to `storeService.suspend(storeId, _reason)` — and **never persisted** (only a `logger.warn`). `PUT /admin/vendors/:id/suspend` has no zod at all. Owner-suspend / clear-payment-config / clear-domain / reset-2FA fire on a single click with no confirmation and hardcoded reasons. | `stores/page.tsx:896-997`, `vendors.routes.ts:336-344`, `store.service.ts` | Add `suspension_reason`/`suspended_by`/`suspended_at` columns; add zod; reuse the existing inline-confirm pattern for every irreversible action. |
| B-60 | ~12 admin endpoints have **no zod at all** — every handler in `subscription-lifecycle.routes.ts` destructures `req.body` raw. The worst is `POST /admin/subscription-orders/webhook-resolver` (`:477`), which lets an admin inject an arbitrary `gateway`/`event_type`/`payload` that reaches `settleWebhook` and **can capture a subscription without a real payment**. `createAdjustment` (`:127`) does `Number(amount)` where `NaN` passes the `amount <= 0` guard. | `admin/subscription-lifecycle.routes.ts`, `admin/subscription-orders.routes.ts:275-477` | Zod on all of them; SuperAdmin + strict schema on `webhook-resolver`, or delete it. |
| B-61 | `admin/audit-log.routes.ts:477, 492` — CSV export uses `'…created_at\\n'` and `.join('\\n')` inside single quotes, so the file is **one line containing literal `\n`**. Unusable in Excel. Export also returns `LIMIT 10000` in one response. `DELETE /admin/audit-log/purge` requires only Admin — an admin can erase their own trail. | same file | `'\n'`; stream the export; `requireSuperAdmin` on purge. |
| B-62 | `seller.route.ts:22-56` reads `req.query.store_id \|\| req.body.store_id \|\| req.headers['x-store-id']` and for admin/super_admin returns it **with no ownership check**. Currently unreachable because `requireStore` runs first on all six routes, but it would let an admin broadcast to an arbitrary store's subscribers the moment someone mounts a route without it. | `api/seller.route.ts:22-56` | Delete the fallback; use `req.user!.store_id!` like every other seller route. |
| B-63 | Ads console moves money through `window.prompt` — promotional credit amount (`:213`), balance adjustment accepting negatives (`:231`), refund reason (`:288`), transaction details in `alert()` (`:705`). Idempotency keys are `promo-${account.id}-${Date.now()}`, i.e. a timestamp, giving no protection against a double-click. `orders/page.tsx:1796` uses `window.prompt` to cancel a fulfilment. | `(admin)/ads/page.tsx`, `hub/dashboard/orders/page.tsx` | Modal forms; `crypto.randomUUID()` generated once per form open. |
| B-64 | Any query string makes product and catalog pages `noindex` — `hasStorefrontQueryParams` returns true for **any** non-empty param, so `?utm_source=facebook` or `?fbclid=…` de-indexes a shared product link. | `lib/storefront-seo.ts:57-62` | Only treat catalog-state keys (`STOREFRONT_CATALOG_QUERY_KEYS` + `page`) as index-suppressing; ignore tracking params. |
| B-65 | Hub search is `'use client'` at the route root: no metadata, no SSR, no robots directive, three sequential client round-trips. One request per keystroke with no debounce and no `AbortController` (out-of-order responses overwrite newer ones). Sort change doesn't reset `page`. Filter state never enters the URL — results are not shareable and Back doesn't work. Category pages link to search with `?category=${category.name}` while search compares against `cat.slug`. | `hub/search/page.tsx:1, 126-251, 720-729`; `hub/category/[slug]/page.tsx:367, 381, 451` | Server component shell + client filter child; debounce + abort + sequence guard; `setPage(1)` on sort; sync state to `searchParams`; pass `category.slug`. |
| B-66 | `hub/pages/[slug]` has `revalidate = 0` (dynamic every request) **and** `next:{revalidate:60}` on the fetch — pays full SSR cost yet stays up to 60 s stale. `generateMetadata` always uses the non-preview fetch, so preview URLs render `title:'Page Not Found'` with no `noindex`. The page renders only `SafePageRenderer` — **no navbar, no footer, no theme**. Live: `/hub/pages/about` → 404 (0 platform CMS pages exist). | `hub/pages/[slug]/page.tsx:8, 23, 52-88` | Pick one caching strategy; use the preview fetch in metadata + `noindex`; wrap in `HubNavbar`/`HubFooter`. |
| B-67 | No `loading.tsx` anywhere in `frontend/src`. Every server route blocks on uncached fetches with no streamed fallback. Homepage uses `cache:'no-store'` on the feed, and its 12 s abort signal is attached only to the primary request while the fallback has none — plus if the primary *throws*, the fallback is never attempted. | `hub/page.tsx:108-201` and all server routes | Add `loading.tsx` skeletons; one `Promise.all` with a shared `AbortSignal.timeout`; `next:{revalidate:60, tags:['marketplace-feed']}` with tag invalidation. |
| B-68 | Wishlist login CTA is `href="/auth/login"` — a route that does not exist (auth lives at `/login`, `/login/buyer`, …). Mutations ignore the response, so items disappear from the UI on a 500, and `setItems(data.items)` trusts the payload shape (`items.length` throws on an unexpected response). Unbounded pagination renders one button per page. | `hub/wishlist/page.tsx:57-90, 138, 248-263` | `/login/buyer?next=/hub/wishlist`; check responses; windowed pagination. |
| B-69 | Logged-out users on `/hub/orders` see "Aucune commande trouvée" instead of a login prompt (only `res.ok` handled; 401 leaves the list empty). The order expand/collapse is a `<div onClick>` with no `role`, `tabIndex`, `onKeyDown` or `aria-expanded` — the entire disclosure is keyboard-inaccessible. The report modal has no `role="dialog"`, focus trap, or Escape handler. Currency hardcoded `TND` while `Order.currency` is available and unused. | `hub/orders/page.tsx:170-256, 280-308, 422-469` | 401 → redirect; `<button aria-expanded aria-controls>`; proper dialog semantics; use `order.currency`. |
| B-70 | `hub/account` gates auth client-side: calls `/auth/me` in an effect and bounces with `window.location.href` on **any** error — a transient network blip logs the user out of the UI. Orders fetch is serialized behind it. | `hub/account/page.tsx:36-78` | Gate in a server component with `redirect()`; fetch user+orders in parallel. |
| B-71 | Icon-only buttons without `aria-label` across hub search (6), orders (2), cart (3), wishlist (1) — several rely on `title`, which is not a reliable accessible name. | listed in the frontend findings | Add localised `aria-label`; `aria-hidden="true"` on decorative icons. |
| B-72 | `<img>` instead of `next/image` on hub cart (`:210`) and wishlist (`:191`); search renders product images as a CSS `background-image` on a `role="img"` div (`:888-896`), skipping the image pipeline and preventing LCP preload. `next.config.ts:41-111` already has `remotePatterns` configured. | those files | Migrate to `next/image` with explicit dimensions. |
| B-73 | `webhooks/page.tsx:74, 90` use bare `fetch` instead of `fetchWithCsrf`, so they miss the silent 401→refresh→retry — an expired token renders "No webhooks" instead of recovering. Event list offers 7 of 8 supported events (`pd.order.delivered` missing). API-keys page offers `read:customers`, a scope no endpoint enforces. | `hub/dashboard/webhooks/page.tsx`, `api-keys/page.tsx:32` | `fetchWithCsrf`; add the missing event; remove or enforce the scope. |
| B-74 | Duplicate/orphan surfaces: `wallet/page.tsx` and `financial/page.tsx` tab `wallet` implement the same UI against the same endpoints, both in the sidebar. `online-store/domains` duplicates the settings Domain tab (only one is gated). `online-store/themes` duplicates the settings Theme tab (only one is gated). `smtp-config` is more capable than the Settings→Email tab and **saving from Settings silently reverts a Brevo-API configuration to plain SMTP** (`email_transport` omitted, backend defaults it to `'smtp'`). `support/page.tsx` and `subscription/payment-method` have no nav entry. `(admin)/cms`, `platform-media`, `smtp-config`, `admin/support` have no sidebar entry. | multiple | Consolidate to one surface per capability; make omitted `email_transport` mean "keep existing" like `smtp_pass` already does. |
| B-75 | `online-store/page.tsx:200-221` — the publish button shows the *current* state ("Published", green) and clicking it sends `enabled:true`, i.e. the green "Published" button **un-publishes** the store. No confirmation for hiding an entire storefront. | that file | Label the action, not the state; confirm. |
| B-76 | Product delete/status changes mutate local state and decrement `totalProducts` without refreshing `storeCounts`, so the KPI tiles go stale after every inline action (bulk actions do it correctly with `await fetchProducts()`). | `products/page.tsx:2741-2779` | Refetch after mutation. |
| B-77 | Search: `searchService` (Meilisearch + a Postgres fallback) is **never called by the query path**. `search.route.ts` uses `productService.searchPublished`/`listPublished`. `searchService` is referenced only by `product.subscriber.ts:28` (indexing) and `search-runner.ts` (reindex). So the whole `searchProducts`/`searchProductsPostgres` code path — including the P1-8 fix from the prior audit — is dead code. | `services/search.service.ts`, `api/search.route.ts` | Decide: either route queries through `searchService` (so enabling Meili actually changes behaviour) or delete the query methods and keep it as an indexer only. |
| B-78 | Bulk fulfilment is a sequential client-side N+1 (`for … await` one POST each, 50 orders = 50 serial round-trips, no progress indicator). `products/[...segments]` probes each URL segment with a separate backend round-trip. | `orders/page.tsx:2009-2021`, `products/[...segments]/page.tsx:111-122` | Server-side bulk endpoint; single resolve call. |
| B-79 | `products/page.tsx` is one 7,358-line client component: the entire drawer form lives in page state, so every keystroke in the product editor re-renders the KPI cards, table, pagination and bulk bar; the keyboard-shortcut effect re-registers on every change. Two identical effects fetch the list twice on every filter change (`:956` and `:1005`). `?edit=<id>` deep links only work if the product is on the current page. | that file | Split the drawer into its own component with local state; remove the duplicate effect; fetch the product by id for deep links. |
| B-80 | `db/pool.ts:66-81` — if `ROLLBACK` throws (broken connection mid-transaction) the rollback error **replaces** the real one and `client.release()` returns a possibly-still-in-transaction connection to the pool. No `idle_in_transaction_session_timeout`, which matters given B-24's long `FOR UPDATE` loops. | that file | `try { ROLLBACK } catch {}` then rethrow the original; `client.release(true)` on rollback failure; set the timeout. |
| B-81 | Money is JS floats end to end. `toMinorUnits` returns a bigint but nothing in the commission path uses it, and it silently assumes a 100 scale for non-TND (wrong for JOD/KWD/BHD/OMR). `order.subscriber.ts:179-196` computes commission per store independently, so the sum of vendor nets + commissions can drift from the order total by a millime per store. DB columns are correctly `NUMERIC(12,3)`. | `utils/money.ts:11, 40-52`, `order.subscriber.ts` | Do all money math in integer minor units; allocate the rounding residual to a designated party. |
| B-82 | Registration confirms whether an email exists (`AUTH_EMAIL_EXISTS` with the email echoed in `details`) — full marketplace-wide and per-store customer enumeration. `forgotPassword` is correctly silent. | `auth.service.ts:175-182`, `storefront-auth.service.ts:176-179` | Generic 202 + an "account already exists" email. |
| B-83 | Auth cookies are `sameSite:'lax'` and `secure` is gated on a raw `process.env.PD_NODE_ENV` read rather than `config.env` — a deployment setting only `NODE_ENV=production` (which `config.ts:73-74` accommodates) ships non-secure cookies. | `api/auth.route.ts:117-136` | Use `config.env`; `sameSite:'strict'` for `pd_rt`. |
| B-84 | Middleware host caches are **fully cleared** when full (`cache.clear()` at 1000 entries), and `classifyHost` treats any unknown Host as a storefront — spraying 1000 Host headers flushes the cache and forces a backend round-trip per request. | `frontend/src/middleware.ts:187` | Evict oldest; skip caching hosts failing a hostname-shape check. |
| B-85 | Per-store `robots.txt`/`sitemap.xml` route handlers are unreachable from tenant hosts (the middleware matcher excludes any segment containing a dot), so the root `app/robots.ts`/`app/sitemap.ts` serve them — and the `[storeHost]` variants emit wrong `Disallow` paths. Two implementations of the same rules. | `store/[storeHost]/robots.txt/route.ts`, `sitemap.xml/route.ts` | Delete the two `[storeHost]` handlers; keep the root metadata routes. |
| B-86 | `checkout/status` is display-only and never reads order state; `checkout/success` and `checkout/status` have no `isMarketplaceHost` redirect (cart and checkout do), so on `/store/{host}/checkout/success` every root-absolute link leaves the tenant. `storeBaseHref = ''` is hardcoded in storefront cart and checkout. | those files | Derive the base with `getStoreRouteContext`; add the redirect; read real order state. |
| B-87 | Account layout can redirect to the wrong login URL — `routeBase` is set in an effect, so the first pass of `loadAuthAndStore` runs with `''` and sends an unauthenticated visitor to `/login` instead of `/store/{host}/login`. | `store/[storeHost]/account/layout.tsx:37-73` | Compute the base synchronously from `useParams()`/`usePathname()`. |
| B-88 | `product/[slug]/page.tsx:246-248` builds a `tx` translator and never calls it (also an unused-variable lint failure). Product JSON-LD omits the `aggregateRating` already fetched at `:260-264`. Hub PDP has **no** `Product`/`Offer`/`AggregateRating`/`BreadcrumbList` JSON-LD at all despite having every field in hand. | those files | Remove the dead `tx`; add `aggregateRating`; add PDP + category structured data. |
| B-89 | `useCheckoutQuote` nulls the quote on every dependency change, so each address keystroke flips the total to `'—'` for ≥350 ms + round-trip. Generic failures (`TypeError: Failed to fetch`) are cast to `CheckoutRequestError` and rendered verbatim to the buyer. `submitCheckoutOrder` accepts no `signal`, so a hung request leaves the button disabled forever. `normalizeCheckoutAddress` fabricates `last_name = first_name` for a single-token name and hardcodes `country:'TN'`. | `hooks/useCheckoutQuote.ts:57-84`, `lib/checkout-quote.ts:190-204, 336-376` | Keep the previous quote with a `stale` flag; map non-domain errors to `t('errors.networkError')`; add a timeout signal; split first/last name fields. |
| B-90 | 1 failing frontend test: `src/__tests__/admin-ads-page.test.tsx > submits block IP form correctly` (`:110`). 421/422 otherwise green. | that test | Fix the assertion or the component; CI currently blocks on `vitest run`, so this is a red gate. |
| B-91 | E2E workflow is `continue-on-error: true` (advisory) and runs only 4 of 15 specs. The contract audit has 12 entries on its `IGNORE` list, several of which are real gaps (`GET /storefront/account/me` is now implemented in your working tree but still ignored; `POST /retention/rewards-lead` likewise). | `.github/workflows/e2e.yml`, `scripts/api-contract-audit.cjs` | Flip to blocking after the first green run; prune `IGNORE` entries whose routes now exist. |
| B-92 | Repo hygiene: root has 12 scratch/audit markdown files plus `h -c git diff --stat 2?&1` (a shell-typo artefact), `env-vars.json`, `eslint-fe.json` duplicated at root and in `frontend/`, `.gitlab-ci.yml` alongside GitHub Actions, and four untracked audit folders. 1,354 refresh tokens (1,281 expired) and 3,779 audit-log rows with no retention job. `pd_system_log` has 733 rows, all `error`. | repo root, DB | `git rm` the scratch files; gitignore `playwright-report/`, `test-results/`; add retention jobs for `pd_refresh_tokens`, `pd_audit_log`, `pd_system_log`, `pd_ads_transaction`, `pd_marketplace_analytics_event`. |

